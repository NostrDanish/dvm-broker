# dvm-broker

Request compute from Nostr DVMs (NIP-90) and discover handlers (NIP-89, kind:31990).
Pays DVMs via your NWC string on job completion.

## Setup
```
npm install @nostr-dev-kit/ndk nostrwalletconnect
node broker.js --prompt "explain Nostr to a cypherpunk" --kind 5050
```
